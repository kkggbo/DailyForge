# DailyForge 项目交接文档

本文档用于帮助后续开发 Agent 快速接手 DailyForge。接手时应先阅读本文件，再按需阅读 `AGENTS.md`、`docs/agent协作规范.md`、各模块 PRD / 接口文档 / DDD。

## 1. 项目定位

DailyForge 是一个面向健身训练场景的 Web 应用。当前 MVP 重点是：

- 用户注册、登录与 AI 权限授权
- 用户基础档案与身体指标记录
- 系统动作库查询
- 训练周期模板管理
- 训练工作台与训练打卡
- AI 生成训练模板草稿
- AI 分析已完成训练周期并生成总结建议

当前项目还没有进入正式生产部署阶段，本地开发环境以 Docker Compose 启动 MySQL / Redis，数据库迁移脚本暂时手动执行。

## 2. 仓库结构

```text
DailyForge/
├── AGENTS.md                         # 当前项目 Agent 快速执行规则
├── README.md                         # 项目概览和启动说明
├── backend/                          # Spring Boot 后端
├── frontend/                         # React + TypeScript 前端
├── deploy/                           # 本地基础设施 Docker Compose
├── docs/                             # PRD、接口、前后端 DDD、改造清单
├── change-log/                       # 每日开发日志
├── dbdata/                           # 本地导出的调试数据，不应提交
├── skill-drafts/                     # Skill 草稿，已配置 gitignore
└── .gitignore
```

重点目录：

- `backend/src/main/java/com/dailyforge/modules/**`：后端业务模块
- `backend/src/main/resources/db/migration/**`：SQL 迁移脚本
- `backend/src/test/java/com/dailyforge/**`：后端测试
- `frontend/src/app/**`：前端路由、布局、全局 Provider
- `frontend/src/shared/**`：前端共享基础设施
- `frontend/src/features/**`：前端业务功能模块
- `docs/prd/**`：产品需求文档
- `docs/interfaces/**`：接口契约文档
- `docs/backend/**`：后端 DDD 与改造清单
- `docs/frontend/**`：前端 DDD 与页面说明

## 3. 技术选型

### 后端

- Java 21
- Spring Boot 3.2.5
- Spring Security
- JWT
- MyBatis-Plus 3.5.5
- MySQL Connector/J
- Redis
- springdoc-openapi
- JUnit 5 / Spring Boot Test / H2
- Maven

后端入口：

- `backend/src/main/java/com/dailyforge/DailyForgeApplication.java`

后端统一端口与上下文：

- 默认端口：`8080`
- Servlet context path：`/api`
- Swagger UI 配置：`/api/docs/swagger`
- OpenAPI JSON 配置：`/api/docs/api`

### 前端

- React 19
- TypeScript 5.7
- React Router 6
- Vite 6
- Tailwind CSS 4
- Vitest
- React Testing Library
- pnpm

前端入口：

- `frontend/src/main.tsx`
- `frontend/src/app/router.tsx`
- `frontend/src/app/layout/AppShell.tsx`
- `frontend/src/app/providers/AuthProvider.tsx`

### 基础设施

- MySQL 8.0
- Redis 7
- phpMyAdmin
- Docker Compose

本地基础设施文件：

- `deploy/docker-compose.local.yml`
- `deploy/local-services.example.md`

## 4. 本地启动与验证

### 启动基础设施

```powershell
docker compose -f deploy/docker-compose.local.yml up -d
```

默认服务：

- MySQL：`localhost:3306`
- Redis：`localhost:6379`
- phpMyAdmin：`http://localhost:8081`

### 初始化数据库

当前运行时没有启用 Flyway 自动迁移。新环境需要按顺序手动执行：

```text
backend/src/main/resources/db/migration/V1__init_schema.sql
backend/src/main/resources/db/migration/V2__seed_base_data.sql
backend/src/main/resources/db/migration/V3__profile_schema_upgrade.sql
backend/src/main/resources/db/migration/V4__cycle_template_schema_upgrade.sql
backend/src/main/resources/db/migration/V5__cycle_template_structure_v2.sql
backend/src/main/resources/db/migration/V6__workout_schema_upgrade.sql
backend/src/main/resources/db/migration/V7__ai_coach_schema_upgrade.sql
```

说明：

- `V2__seed_base_data.sql` 包含基础动作、肌肉、器械等种子数据。
- H2 测试使用 `backend/src/test/resources/schema-auth.sql`，不依赖本地 MySQL 手动迁移。

### 启动后端

```powershell
cd backend
mvn spring-boot:run
```

后端默认读取：

- `backend/src/main/resources/application.yml`
- `backend/src/main/resources/application-dev.yml`

AI API Key 使用环境变量：

```powershell
$env:DAILYFORGE_AI_API_KEY="你的 DeepSeek API Key"
```

### 启动前端

```powershell
cd frontend
pnpm install
pnpm dev
```

前端默认通过 `/api/**` 访问后端。

### 常用测试命令

```powershell
cd backend
mvn test
```

```powershell
cd frontend
pnpm test:run
pnpm build
```

## 5. 后端架构约定

后端采用按业务模块分包的分层结构。以 `workout` / `aicoach` 为例：

```text
modules/<module>/
├── interfaces/
│   ├── rest/                         # Controller
│   ├── dto/                          # 请求 DTO
│   └── vo/                           # 响应 VO
├── application/
│   ├── service/                      # 应用服务，事务与流程编排
│   └── assembler/                    # Entity / 领域结果 / VO 转换
├── domain/
│   ├── model/                        # 领域枚举和值对象
│   └── service/                      # 业务规则校验
└── infrastructure/
    ├── persistence/
    │   ├── entity/                   # MyBatis-Plus Entity
    │   └── mapper/                   # Mapper 接口与 SQL
    └── ai/                           # AI 模型客户端、Prompt、Tool、Executor
```

关键约定：

- Controller 不直接返回 Entity。
- Controller 统一返回 `ApiResponse<T>`。
- 业务异常使用 `BusinessException` + `ErrorCode`。
- 通用异常由 `GlobalExceptionHandler` 处理。
- 当前登录用户通过 `AuthSecurityUtils` / `AuthUserPrincipal` 获取。
- MyBatis-Plus 默认开启下划线到驼峰映射。
- 新增接口必须同步更新 `docs/interfaces/**` 和 Swagger 注解。
- 新增业务规则优先放在 domain service，应用服务负责事务与流程编排。

重要公共类：

- `backend/src/main/java/com/dailyforge/common/ApiResponse.java`
- `backend/src/main/java/com/dailyforge/common/ErrorCode.java`
- `backend/src/main/java/com/dailyforge/common/BusinessException.java`
- `backend/src/main/java/com/dailyforge/common/handler/GlobalExceptionHandler.java`
- `backend/src/main/java/com/dailyforge/config/SecurityConfig.java`
- `backend/src/main/java/com/dailyforge/infrastructure/security/JwtTokenService.java`

## 6. 前端架构约定

前端按 feature 组织代码：

```text
frontend/src/features/<feature>/
├── api/                              # API client
├── components/                       # 业务组件
├── hooks/                            # 复杂页面状态 Hook
├── lib/                              # mapper、formatter、enum、validator
├── pages/                            # 路由页面
└── types/                            # TypeScript 类型
```

关键约定：

- API client 通过 `frontend/src/shared/api/http.ts` 的 `request<T>` 访问后端。
- 后端响应体会在 `request<T>` 中解包，只向 feature 返回 `data`。
- 前端类型放在 feature 的 `types/**`，不要直接散落在页面文件中。
- 页面负责路由级状态与组合，复杂编辑逻辑优先抽到 hook 或组件。
- 表单、状态文案、错误提示要和接口文档中的错误码语义对齐。
- 测试使用 Vitest + React Testing Library，现有测试以 `*.test.tsx` 为主。
- 当前项目 UI 已形成偏产品工具型的 Tailwind 风格，新增页面应延续现有导航、按钮、卡片和状态展示方式。

重要前端基础文件：

- `frontend/src/app/router.tsx`
- `frontend/src/app/layout/AppShell.tsx`
- `frontend/src/app/providers/AuthProvider.tsx`
- `frontend/src/shared/api/http.ts`
- `frontend/src/styles/index.css`

## 7. 已实现模块状态

### auth

后端模块：`backend/src/main/java/com/dailyforge/modules/auth/**`

前端模块：`frontend/src/features/auth/**`

已实现能力：

- 注册
- 登录
- 获取当前用户
- 刷新 Token
- 登出
- 邀请码兑换
- AI 权限通过邀请码提升账户 tier

主要接口：

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/refresh-token`
- `POST /api/auth/logout`
- `POST /api/auth/redeem-invite-code`

参考文档：

- `docs/interfaces/auth_接口文档.md`
- `docs/backend/auth_module/auth_DDD.md`
- `docs/frontend/auth_module/auth_DDD.md`
- `docs/frontend/auth_module/auth_页面实现.md`

### profile

后端模块：`backend/src/main/java/com/dailyforge/modules/profile/**`

前端模块：`frontend/src/features/profile/**`

已实现能力：

- 基础档案按需填写
- 首次注册后欢迎引导，可跳过
- 身体指标新增记录
- 当前身体指标快照表
- 删除最近一条身体指标记录
- AI 使用前档案完整度摘要

主要接口：

- `GET /api/profile/basic`
- `PUT /api/profile/basic`
- `GET /api/profile/body-metrics/current`
- `GET /api/profile/body-metrics`
- `POST /api/profile/body-metrics`
- `DELETE /api/profile/body-metrics/latest`
- `GET /api/profile/completion-summary`

参考文档：

- `docs/prd/profile_PRD.md`
- `docs/interfaces/profile_接口文档.md`
- `docs/backend/profile_module/profile_DDD.md`
- `docs/frontend/profile_module/profile_DDD.md`

### exercise

后端模块：`backend/src/main/java/com/dailyforge/modules/exercise/**`

前端模块：`frontend/src/features/exercise/**`

已实现能力：

- 系统动作库查询
- 分类、肌肉、器械筛选
- 关键词搜索
- 动作详情
- 动作选择器支持粗粒度分类和细分肌肉

主要接口：

- `GET /api/exercises/system/filter-options`
- `GET /api/exercises/system`
- `GET /api/exercises/system/{exerciseId}`

参考文档：

- `docs/interfaces/exercise_接口文档.md`
- `docs/backend/exercise_module/exercise_DDD.md`
- `docs/frontend/exercise_module/exercise_DDD.md`

### cycle_template

后端模块名为 `plan`：`backend/src/main/java/com/dailyforge/modules/plan/**`

前端模块：`frontend/src/features/cycle-template/**`

已实现能力：

- 正式模板和草稿模板双 Tab
- 多草稿并存
- 同一时间只允许一个 active 模板
- 模板状态：`draft`、`active`、`inactive`、`deleted`
- 周期长度严格限制 1 到 7 天
- Day 默认命名并允许改名
- 允许空白天，空白天视为休息日
- 动作只能从系统动作库选择
- 动作结构为 `动作 -> 执行项 -> 参数`
- 参数支持组、重量、次数、时长、休息、RPE、备注等结构化建模
- 正式模板启用时会切换当前训练循环
- active 模板修改需要确认，并会同步影响当前未完成训练内容
- 模板软删除，历史训练记录保留
- AI 生成模板有 `AI生成` 标识

主要接口：

- `GET /api/cycle-templates/formal`
- `GET /api/cycle-templates/drafts`
- `GET /api/cycle-templates/{templateId}`
- `POST /api/cycle-templates/drafts`
- `PUT /api/cycle-templates/drafts/{templateId}`
- `PUT /api/cycle-templates/{templateId}`
- `POST /api/cycle-templates/{templateId}/copy`
- `POST /api/cycle-templates/{templateId}/activate`
- `GET /api/cycle-templates/active/current`
- `DELETE /api/cycle-templates/{templateId}`

参考文档：

- `docs/prd/cycle_template_PRD.md`
- `docs/interfaces/cycle_template_接口文档_v2.md`
- `docs/backend/cycle_template_module/cycle_template_DDD_v2.md`
- `docs/frontend/cycle_template_module/cycle_template_DDD.md`
- `docs/动作参数模型改造草案.md`

### workout

后端模块：`backend/src/main/java/com/dailyforge/modules/workout/**`

前端模块：`frontend/src/features/workout/**`

已实现能力：

- 进入训练页时自动展示当前 Day
- 无 in-progress session 时自动创建当前 Day session
- 已有 session 时直接展示，不重复创建
- Day 导航支持历史只读、当前可编辑、未来预览
- 浏览未来 Day 不创建 session
- 手动保存训练数据
- 完成打卡后停留当前 Day
- 刷新或重新进入时跳转到最新未完成 Day
- 支持训练日和休息日完成
- 训练记录使用模板快照，历史记录不受后续模板编辑影响
- 当前周期结束后提供重启同模板、选择其他模板、AI 周期总结入口
- 模板切换时旧 cycle run 进入 `cancelled`

主要接口：

- `GET /api/workouts/context`
- `POST /api/workouts/current-day/session`
- `GET /api/workouts/days/{dayIndex}`
- `PUT /api/workouts/sessions/{sessionId}`
- `POST /api/workouts/sessions/{sessionId}/complete`
- `GET /api/workouts/sessions/{sessionId}`
- `GET /api/workouts/recent`
- `POST /api/workouts/cycles/current/restart`
- `POST /api/workouts/cycles/current/ai-analysis`

参考文档：

- `docs/prd/workout_PRD.md`
- `docs/interfaces/workout_接口文档.md`
- `docs/backend/workout_module/workout_business_flow.md`
- `docs/frontend/workout_module/workout_DDD.md`
- `docs/testing/workout_功能测试顺序建议.md`

### ai_coach

后端模块：`backend/src/main/java/com/dailyforge/modules/aicoach/**`

前端模块：`frontend/src/features/ai-coach/**`

已实现能力：

- AI 能力入口与权限判断
- AI 生成训练模板草稿
- AI 周期总结
- AI 任务异步执行与轮询
- AI 任务历史列表
- 已生成模板 / 总结详情回看
- 当前周期已有总结时优先打开历史总结
- 最近工具调用展示中文名称
- AI 输出 JSON 校验与最多 2 次修复
- DeepSeek OpenAI 兼容接口真实调用

主要接口：

- `GET /api/ai-coach/capabilities`
- `POST /api/ai-coach/template-generations`
- `GET /api/ai-coach/template-generations/{taskId}`
- `GET /api/ai-coach/template-generations/history`
- `POST /api/ai-coach/cycle-summaries`
- `GET /api/ai-coach/cycle-summaries/{taskId}`
- `GET /api/ai-coach/cycle-summaries/history`
- `GET /api/ai-coach/cycle-summaries/latest-by-cycle-run/{cycleRunId}`

AI 配置：

- `dailyforge.ai.enabled`
- `dailyforge.ai.provider`
- `dailyforge.ai.model`
- `dailyforge.ai.base-url`
- `dailyforge.ai.api-key`
- `dailyforge.ai.timeout`
- `dailyforge.ai.max-tool-rounds`
- `dailyforge.ai.max-repair-attempts`
- `dailyforge.ai.template-generation-prompt-version`
- `dailyforge.ai.cycle-summary-prompt-version`

当前默认：

- provider：`deepseek`
- model：`deepseek-v4-pro`
- timeout：`PT120S`
- max tool rounds：`50`
- repair attempts：`2`

参考文档：

- `docs/prd/ai_coach_PRD.md`
- `docs/interfaces/ai_coach_接口文档.md`
- `docs/backend/ai_coach_module/ai_coach_DDD.md`
- `docs/backend/ai_coach_module/AI接入与提示词上下文设计.md`
- `docs/frontend/ai_coach_module/ai_coach_DDD.md`

## 8. 数据库设计要点

当前迁移脚本已经覆盖：

- 用户、邀请码、权限等级
- 用户基础档案与身体指标
- 系统动作库、动作肌肉关系、器械关系
- 训练模板、模板版本、模板天、模板动作、执行项、参数
- 用户当前激活周期、训练周期运行记录
- 训练 session、训练动作快照、执行项快照、参数实际值
- AI task record、AI tool call record

关键表语义：

- `cycle_templates`：模板主表，维护状态和当前版本引用。
- `cycle_template_versions`：模板版本表，避免历史训练直接依赖可变模板。
- `cycle_template_days`：模板中的 Day。
- `cycle_day_exercises`：模板 Day 下的动作。
- `cycle_day_exercise_items`：动作下的执行项，例如某一组、某一段。
- `cycle_day_exercise_item_metrics`：执行项下的参数，例如重量、次数、时长。
- `cycle_runs`：用户基于某个模板版本开启的一轮训练周期。
- `training_sessions`：某个 cycle run 中某一天的训练会话。
- `training_session_exercises`：训练会话中的动作快照。
- `training_session_exercise_items`：训练执行项快照。
- `training_session_exercise_item_metrics`：训练执行项的计划值与实际值。
- `ai_task_records`：AI 任务状态、请求快照、结果摘要、错误信息。
- `ai_task_tool_calls`：AI 调用工具的记录，用于进度展示和排查。

注意：

- 运行时 Flyway 已关闭，不要假设应用启动会自动建表。
- 新增 schema 变更必须新增有序 SQL 脚本，不要直接改旧迁移作为正式方案。
- 当前项目早期曾遇到 MySQL 与 Flyway 版本不兼容，因此迁移策略暂时是手动 SQL。

## 9. 文档风格与开发流程

文档优先级：

1. 用户在当前轮次最新确认的决定
2. `AGENTS.md`
3. `docs/agent协作规范.md`
4. `docs/prd/**`
5. `docs/interfaces/**`
6. `docs/backend/**`
7. `docs/frontend/**`

新模块推荐流程：

1. 先确认需求和边界。
2. 产出或更新 PRD。
3. 产出或更新接口文档。
4. 如涉及数据库，先产出数据库改造清单和 SQL。
5. 后端按 DDD / Controller / Service / Domain / Mapper 落地。
6. 前端按 feature 模块落地页面、组件、API、类型。
7. 补测试。
8. 做契约检查、代码审查、README/changelog 更新。
9. 再提交。

文档风格：

- 中文为主，标题清晰。
- PRD 关注用户流程、业务规则、范围、非目标、验收标准。
- 接口文档要写清路径、方法、认证、请求参数、响应字段、错误码、联调约束。
- DDD 要写清模块职责、分层、核心流程、状态机、异常、测试策略。
- 改造清单要拆成前端 / 后端 / 数据库可执行任务，便于不同 agent 分工。

## 10. Agent 协作规则

本项目已经沉淀了固定 Agent 角色与 Skill。接手 Agent 必须优先阅读：

- `AGENTS.md`
- `docs/agent协作规范.md`

当前全局持久化 Agent 位于用户目录：

```text
C:\Users\kkggbo\.codex\agents\
```

已配置角色：

- `dailyforge-product-manager`
- `dailyforge-frontend-architect`
- `dailyforge-backend-builder`
- `dailyforge-data-layer`
- `dailyforge-dto-mapper`
- `dailyforge-test-generator`
- `dailyforge-quality-guardian`
- `dailyforge-git-steward`

协作原则：

- 主控会话负责需求确认、文档、任务拆解、契约校验和最终收敛。
- 实现阶段按前端、后端、数据层、DTO 等职责拆分。
- 同角色默认复用单实例，不为了形式重复创建 subagent。
- 小阻塞由主控本地处理。
- 阶段结束后关闭不再需要的 subagent。
- 提交前需要测试、审查、README/changelog 同步和 Git Steward 复核。

## 11. 代码风格

### Java

- 包结构遵循现有模块分层。
- DTO / VO / Entity 分离。
- 不直接向 Controller 暴露 Entity。
- 应用服务负责事务和跨聚合流程。
- 领域服务负责业务规则判断。
- 错误码统一加入 `ErrorCode`。
- 异常统一使用 `BusinessException`，除非是框架级异常。
- 复杂业务逻辑可以加中文注释，注释应说明为什么这样处理。

### TypeScript / React

- 使用函数组件与 Hooks。
- 类型定义放在 `types/**`。
- API 请求放在 `api/**`。
- 格式化、枚举、mapper、validator 放在 `lib/**`。
- 组件 Props 应显式定义 interface。
- 不把接口响应的原始结构散落到多个组件中。
- 页面文案、错误状态和交互流程要和接口文档保持一致。

### SQL

- 表使用 InnoDB、`utf8mb4`。
- 长索引字段注意 MySQL 8.0 下的索引长度与 row format。
- 新表必须考虑查询路径和索引。
- 删除尽量使用软删除或状态字段，除非业务明确允许物理删除。
- 历史训练记录必须保留快照语义。

## 12. 测试现状

后端已有测试覆盖：

- `AuthIntegrationTest`
- `ProfileIntegrationTest`
- `ExerciseIntegrationTest`
- `CycleTemplateIntegrationTest`
- `WorkoutIntegrationTest`
- AI Coach application service / prompt / tool support 测试
- 多个 domain service 单元测试

前端已有测试覆盖：

- auth 登录 / 注册页面
- profile 表单、指标记录、完成度提示
- exercise API
- cycle-template 编辑器、动作选择器、只读展示、hook
- workout 页面和 AI 总结入口
- ai-coach 首页、生成页、任务详情页、历史页、周期总结页

后续新增功能时，优先补：

- 后端应用服务关键分支测试
- Controller 接口绑定与错误码测试
- 前端用户关键交互测试
- API client 与类型映射测试

## 13. 当前已知注意事项

- 当前工作区存在未提交的本地文件或目录：`.m2/`、`dbdata/`、`frontend/tsconfig.app.tsbuildinfo`。这些通常不应进入业务提交。
- `dbdata/` 是用户导出的 AI 联调数据，主要用于排查，不是正式种子数据。
- `frontend/tsconfig.*.tsbuildinfo` 是 TypeScript 构建缓存，提交前应确认是否应加入 `.gitignore` 或保持不提交。
- 中文文档和源码注释必须保持 UTF-8。项目曾出现 README / changelog 乱码问题，后续不要用错误编码读取后再覆盖写回。
- PowerShell 终端输出中文可能显示异常，不能仅凭终端显示判断文件实际编码；必要时检查文件字节或在 IDE 中确认。
- AI Key 不应提交到仓库。`application.yml` 中保留占位值，通过环境变量 `DAILYFORGE_AI_API_KEY` 注入。
- 当前 Swagger 路径受 `server.servlet.context-path=/api` 影响。
- 训练模板和训练记录之间存在快照边界，改模板时不要直接改历史训练记录。
- AI 生成模板最终会落为 cycle template draft，用户确认启用后才影响训练周期。

## 14. 推荐下一步开发方向

当前 AI 训练模板生成和 AI 周期总结已经跑通，后续可以从以下方向继续：

- 优化 AI 输出质量：休息日安排、动作编排、中文稳定性、训练强度解释。
- 引入训练统计：按周期、动作、肌群、重量、次数、完成率做趋势分析。
- 开发饮食模块：饮食目标、餐食记录、基础营养建议、AI 饮食建议。
- 增强动作库：动作视频、动作注意事项、替代动作、按器械条件筛选。
- 优化个人档案：训练目标变化记录、伤病注意事项在 AI Prompt 中更细粒度使用。
- 完善部署方案：生产 docker compose、环境变量模板、数据库备份、日志与监控。

## 15. 接手建议

新 Agent 接手后的建议顺序：

1. 读取 `AGENTS.md` 和 `docs/agent协作规范.md`，确认协作与提交规则。
2. 读取 `README.md` 和本文档，建立项目全局视图。
3. 根据要开发的模块读取对应 PRD、接口文档、前后端 DDD。
4. 先跑后端 `mvn test` 和前端 `pnpm test:run`，确认接手时基线状态。
5. 修改前先确认是否涉及数据库迁移和接口契约变更。
6. 实现后更新对应文档、测试、README/changelog。
7. 提交前只暂存本轮相关文件，避免混入本地调试数据和构建缓存。
