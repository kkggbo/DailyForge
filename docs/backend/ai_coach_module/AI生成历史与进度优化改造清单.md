# DailyForge AI 生成历史与进度优化改造清�?
> 模块归属：`backend / frontend / ai_coach`
>
> 适用范围：AI 训练模板生成、AI 周期总结
>
> 目标：补�?AI 历史入口、结果回看、进度可视化、中文输出约束、模板补充输入与休息日策�?
---

## 1. Planning Basis

当前 `ai_coach` 模块已经具备以下能力�?
- `GET /api/ai-coach/capabilities`
- `POST /api/ai-coach/template-generations`
- `GET /api/ai-coach/template-generations/{taskId}`
- `POST /api/ai-coach/cycle-summaries`
- `GET /api/ai-coach/cycle-summaries/{taskId}`

现有实现已可完成�?
- AI 生成训练模板草稿
- AI 生成周期总结
- 结果页轮询与查看

但目前仍缺少�?
- AI 生成历史入口
- 当前周期的“已生成总结”直�?- 更明确的 AI 生成进度展示
- 模板生成自由输入条件
- 中文输出约束与休息日约束的更强提�?
本轮建议尽量**不改数据库结�?*，优先复用：

- `ai_task_records`
- `ai_task_tool_calls`

---

## 2. Iteration Split

### 2.1 必做�?
1. �?AI 生成历史入口
2. 补后端历史查询能�?3. �?prompt 约束
4. 补前端自由输入项

### 2.2 应做�?
5. �?AI 处理中进度展�?
### 2.3 可后置项

6. 历史搜索、筛选、统计视�?7. 更细的任务阶段可视化

---

## 3. Priority Notes

### Must Have

- 模板生成历史可查�?- 周期总结历史可回�?- `workout` 页面点击 AI 周期总结时，若当前周期已有总结结果，优先跳转历史结�?- 模板生成页增加自由输入框
- AI 返回内容尽量强制中文
- AI 模板结果中必须允许休息日

### Should Have

- 任务详情页显示工具调用过�?- 等待页显示阶段状态或进度�?
### Later

- 历史列表筛�?- 历史搜索
- AI 行为统计面板

---

## 4. Frontend Task List

### 4.1 AI 生成主页

- 增加“AI 生成历史”入�?- 历史页建议按 `template_generation` �?`cycle_summary` �?tab
- 保留当前 AI 生成入口

### 4.2 模板生成�?
- 在场景、方向、周期选择外新增自由输入框
- 将自由输入框内容作为本次生成补充条件提交
- 增加“AI 生成模板历史”按�?- 点击后进入模板生成历史列表或展开历史面板

### 4.3 周期总结�?/ Workout �?
- 当当�?`cycle_run` 已存�?AI 周期总结结果时，`AI 周期总结` 按钮优先跳转历史结果
- 不再重复发起新的总结任务
- 保留重新分析的入口，但与“查看历史结果”区分清�?
### 4.4 任务详情�?
- 显示当前任务状�?- 显示最近更新时�?- 显示最近工具调用名称或阶段
- 等待态不要只展示“AI 处理中�?
### 4.5 前端类型

- 扩展任务详情类型，支持进度展示字�?- 扩展历史列表类型
- 扩展模板生成请求类型，增加自由输入字�?
---

## 5. Backend Task List

### 5.1 历史查询能力

- 新增模板生成历史列表查询
- 新增周期总结历史列表查询
- 新增“当�?completed cycle_run 的最�?summary task”查询能�?
### 5.2 任务详情增强

- 扩展任务详情返回�?- 增加最近工具调用信�?- 增加可用于前端展示的阶段状�?
### 5.3 Prompt 约束

- 模板生成 prompt 强制中文输出
- 周期总结 prompt 强制中文输出
- 模板生成 prompt 明确允许休息�?- 模板生成 prompt 支持自由输入补充条件

### 5.4 数据复用

- 优先复用 `ai_task_records` / `ai_task_tool_calls`
- 不建议本轮新增历史专用表

---

## 6. Optional Data / DTO / Test / Review Tasks

### DTO

- 模板生成请求增加自由输入字段
- 历史列表响应补充必要展示字段
- 任务详情响应补充阶段、工具调用、最近更新时�?
### Test

- 后端测试：历史列表、最新周期总结直达、中�?prompt、休息日约束
- 前端测试：历史入口、自由输入框、进度展示、已生成总结直达

### Review

- 检查历史入口是否会误触发重复生�?- 检�?prompt 约束是否真正落到系统提示�?- 检查进度展示是否依赖后端真实状态而非前端猜测

---

## 7. Critical Path and Recommended Execution Order

1. 先定接口契约
2. 再改后端历史查询与任务详�?3. 再改 prompt 约束
4. 再改前端入口与页�?5. 最后补测试与文�?
---

## 8. Suggested Acceptance Criteria

- 模板生成后可从历史入口再次查�?- 周期总结后可从历史入口再次查�?- `workout` 页面不会重复创建已存在的总结任务
- 模板生成支持自由输入补充条件
- AI 输出以中文为默认约束
- 模板结果允许出现休息�?- 等待页可看到任务进度或工具调用痕�?
