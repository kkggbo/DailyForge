server {
    listen 80;
    server_name dailyforge.cn;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name dailyforge.cn;

    ssl_certificate     /etc/nginx/certs/dailyforge.cn.pem;
    ssl_certificate_key /etc/nginx/certs/dailyforge.cn.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback for client-side routing (React Router).
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to the backend (backend servlet context-path is /api,
    # so the original URI /api/... is forwarded unchanged).
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_connect_timeout 60s;
        proxy_read_timeout 120s;
    }

    # Long-lived cache for hashed build assets.
    location /assets/ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
