spring:
  datasource:
    url: jdbc:mysql://mysql:3306/dailyforge?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: dailyforge
    password: change_me
  data:
    redis:
      host: redis
      port: 6379
      database: 0

logging:
  level:
    root: info
    com.dailyforge: info

dailyforge:
  security:
    jwt:
      secret: ${DAILYFORGE_JWT_SECRET:dailyforge-prod-jwt-secret-key-change-me-1234567890}
